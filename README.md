# Docker + XFCE + VSCode + Python + SSH

Imagen Docker con entorno gráfico XFCE, VSCode, Python y SSH.

## Requisitos
- Docker instalado
- Cliente VNC (Remmina/TigerVNC)

## Instrucciones

### 1. Construir la imagen
```bash
docker build -t ubuntu-xfce-vscode .
